services:
  app:
    build: .
    container_name: home-library-app
    restart: unless-stopped
    ports:
      - "${PORT:-4000}:${PORT:-4000}"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/dist
    command: sh -c "npm ci && npm run start:dev"
    env_file:
      - .env
